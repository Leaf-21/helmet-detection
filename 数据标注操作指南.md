# 自标数据操作指南（网上搜图 + labelImg 标注）

> 目标：自己收集并标注 **100~200 张** 工地安全帽图片，作为作业"自制数据集"部分
> （现有 4000 张公开数据训练的模型 mAP 已 0.93 达标，自标数据主要是让作业更完整真实）

---

## 第 1 步：下载图片（网上搜索）

**搜索关键词**（百度图片 / 必应 / 搜狗任选）：
- `工地 工人 安全帽`、`建筑工人 安全帽`、`施工现场 戴安全帽`
- `工人 没戴安全帽`、`工地 工人 头部`

**图片要求**：
| 要求 | 说明 |
|---|---|
| 格式 | JPG / PNG |
| 尺寸 | 建议 ≥ 640×480（太小目标看不清，标不准） |
| 数量 | 100~200 张（每张图可有多个工人） |
| 多样性 | 不同角度、人数、戴帽/不戴帽、明暗环境 |

**下载方式**：图片上右键 → **图片另存为** → 存到 `D:\智能制造\my_dataset\images`（文件名随意，可后续统一改序号）。

> 提示：优先选工人头部清晰、人物居中的图；纯风景/远景图没用。

---

## 第 2 步：建目录并放图

```
D:\智能制造\my_dataset\
├── images\   ← 所有下载的图片放这里
└── labels\   ← labelImg 标注后自动生成（先建好）
```
两个文件夹都先建好。

---

## 第 3 步：labelImg 标注

1. 双击 **`D:\智能制造\start_labelimg.bat`** 打开标注工具；
2. **先按 `Ctrl + +`（Ctrl 加号）**，把格式切到 **YOLO**（窗口状态栏显示 YOLO，保存才是 txt）；
3. 菜单 **Open Dir** → 选 `D:\智能制造\my_dataset\images`；
4. 菜单 **Change Save Dir** → 选 `D:\智能制造\my_dataset\labels`；
5. 开始标注：

| 操作 | 按键 |
|---|---|
| 画框 | `W` → 按住鼠标拖拽 → 松开后选类别 |
| 选类别 | 弹窗里选 `helmet` 或 `head`（下拉框已有） |
| 保存 | `Ctrl+S` |
| 下一张 / 上一张 | `D` / `A` |
| 撤销 | `Ctrl+Z` |
| 删除选中框 | `Delete` |
| 缩放 | `Ctrl+滚轮` |

---

## 第 4 步：标注规则（和公开数据保持一致）

- **helmet**（绿色）：工人**头上戴了**安全帽 → 框住**帽子本身**（不是整个人、不是脸）；
- **head**（红色）：工人**没戴**安全帽 → 框住**头部**（脸+头发区域）；
- 一个人戴帽只标 helmet，没戴只标 head，**不要同时标两个**；
- 框要紧贴目标（稍微留一点边即可，别框半个身体）；
- 太远/太模糊看不清的目标**不标**；
- 每张图可标多个目标，注意画面边缘被截断一半的目标也正常标。

---

## 第 5 步：标完后告诉我

我会帮你：
1. 检查所有标注（格式、类别、坐标是否越界）；
2. 统计每类数量；
3. 把自标数据**合并进训练集**（混入 `helmet_dataset` 后微调训练，或单独统计展示）；
4. 生成作业文档要用的标注截图素材。

---

## 常见问题

| 问题 | 处理 |
|---|---|
| 打开的 labelImg 报 Qt 错误 | 已修复过，若还报错发我截图 |
| 保存出来是 .xml 不是 .txt | 没按 `Ctrl++` 切到 YOLO 格式，先按再标 |
| 图片在 labels 目录找不到对应 txt | 检查 Change Save Dir 是否选对 |
| 觉得 200 张太多 | 100 张即可，质量比数量重要 |
