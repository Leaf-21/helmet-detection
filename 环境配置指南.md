# 环境配置指南（纯浏览器 + 双击脚本版）

> 本版全程不需要手动敲命令：**用浏览器下载 → 双击 .bat 脚本**。
> 电脑：RTX 4060 8GB，Python 已就绪（`D:\智能制造\python310`）。

---

## 你要下载的 4 个文件（用浏览器）

打开 Chrome/Edge，复制下面的链接到地址栏回车即可下载：

| # | 文件 | 大小 | 下载地址 |
|---|---|---|---|
| 1 | torch-2.3.1+cu121-cp310-cp310-win_amd64.whl | 2.4GB | https://mirrors.aliyun.com/pytorch-wheels/cu121/torch-2.3.1%2Bcu121-cp310-cp310-win_amd64.whl |
| 2 | torchvision-0.18.1+cu121-cp310-cp310-win_amd64.whl | 5.7MB | https://mirrors.aliyun.com/pytorch-wheels/cu121/torchvision-0.18.1%2Bcu121-cp310-cp310-win_amd64.whl |
| 3 | yolov5 源码 (master.zip) | ~10MB | https://github.com/ultralytics/yolov5/archive/refs/heads/master.zip |
| 4 | yolov5s.pt 预训练权重 | 14MB | https://github.com/ultralytics/yolov5/releases/download/v6.0/yolov5s.pt |

**下载后放置位置：**
- 文件 1、2 → 放到 `D:\智能制造` 文件夹下（和 install_torch.bat 同目录）
- 文件 3 → 解压到 `D:\智能制造`，**把解压出的 `yolov5-master` 文件夹重命名为 `yolov5`**
- 文件 4 → 放到 `D:\智能制造\yolov5` 文件夹里

> 提示：torch 有 2.4GB，浏览器下载中断可点浏览器下载栏"继续/重试"续传。

---

## 按顺序双击以下脚本（都在 D:\智能制造 下）

| 顺序 | 双击 | 作用 | 成功标志 |
|---|---|---|---|
| 1 | `install_torch.bat` | 安装 PyTorch GPU 版（已内置国内镜像提速，5-15 分钟） | 出现 `torch version: 2.3.1+cu121` + `installed OK` |
| 2 | `check_gpu.bat` | 验证显卡是否被识别 | 出现 `cuda available: True` |
| 3 | `install_yolov5_deps.bat` | 安装 YOLOv5 全部依赖（3-10 分钟） | 出现 `Dependencies installed` |
| 4 | `install_labelimg.bat` | 安装标注工具（2 分钟） | 出现 `labelImg installed` |
| 5 | `start_labelimg.bat` | 打开标注界面（以后随时用） | 弹出图形窗口 |

> **提速说明**：pip 默认从官方 PyPI 下载（实测仅 260KB/s），已改为清华镜像（1.8MB/s，快 7 倍）。
> `install_torch.bat` 已内置镜像配置；也可单独双击 `set_mirror.bat` 随时重配镜像。

> 如果 Windows 弹出蓝色"Windows 已保护你的电脑"提示，点"更多信息 → 仍要运行"。

---

## 检查清单

- [ ] 4 个文件已下载、放对位置（第 3 个解压后记得重命名成 yolov5）
- [ ] 双击 install_torch.bat → 看到 Successfully installed
- [ ] 双击 check_gpu.bat → 看到 cuda available: True ✅ 关键一步
- [ ] 双击 install_yolov5_deps.bat → 看到 Dependencies installed
- [ ] 双击 install_labelimg.bat / start_labelimg.bat → 标注窗口能打开

**全部打勾 = 环境配置完成！** 之后进入"数据集制作"环节（下载公开安全帽数据集或自己拍照标注）。

---

## 遇到问题

| 现象 | 处理 |
|---|---|
| check_gpu.bat 显示 cuda available: False | 说明装成了 CPU 版，重新下载 whl 确认是 `+cu121` 版本再装 |
| install_torch.bat 报找不到 whl 文件 | 文件 1、2 没放到 D:\智能制造，或文件名被浏览器改名（保持 `+` 号原名即可） |
| 双击 bat 一闪而过 | 右键 bat → 编辑，确认内容完整；或按住 Shift 右键 → 用 PowerShell 打开后输入 `.\install_torch.bat` |
| 下载很慢 | 用手机热点/换网络重试；torch 文件可晚上下载 |
| yolov5 依赖安装报错 | 把报错截图发我，多半是某个小包需要换源，我给你单独解决 |
